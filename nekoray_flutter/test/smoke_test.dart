import 'package:flutter_test/flutter_test.dart';

void main() {
  test('smoke test: app package imports', () {
    // Phase-2 MVP smoke test. Real widget tests are added as features land.
    expect(1 + 1, equals(2));
  });
}
